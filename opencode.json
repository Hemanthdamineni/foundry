{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "dev-sdlc": {
      "mode": "primary",
      "prompt": "{file:.opencode/skills/sdlc/SKILL.md}"
    },
    "foundry": {
      "description": "Foundry workspace-aware SDLC orchestration agent with deterministic bootstrap, validation, tracing, and recovery.",
      "mode": "primary",
      "permission": {
        "bash": "ask",
        "edit": "allow",
        "task": "allow"
      },
      "prompt": "{file:foundry/.opencode/agents/foundry.md}"
    }
  },
  "instructions": [
    "When using the foundry agent, ALWAYS use the `question` tool during the Chatting phase to gather user preferences via GUI options. NEVER skip clarification questions. Present 3-4 concrete options with 'Type your own answer' allowed."
  ],
  "mcp": {
    "foundry-orchestrator": {
      "command": [
        "sdlc-mcp"
      ],
      "enabled": true,
      "environment": {},
      "type": "local"
    }
  }
}
